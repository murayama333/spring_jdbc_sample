# Spring JDBC Sample

```sql
create table book(id int primary key, title varchar(100), price int);

insert into book(id, title, price, category_id) values(1, 'HTML Book', 1000);
insert into book(id, title, price, category_id) values(2, 'CSS Book', 2000);
insert into book(id, title, price, category_id) values(3, 'JS Book', 3000);
```
